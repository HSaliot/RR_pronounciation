CREATE TABLE ReadingSelection (
    id INT not null
        GENERATED ALWAYS AS IDENTITY
        (START WITH 1, INCREMENT BY 1),
    title VARCHAR(50) not null,
    text VARCHAR(1024) not null,
    PRIMARY KEY (id)
);
